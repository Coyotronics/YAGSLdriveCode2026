package frc.robot.subsystems;

import edu.wpi.first.math.geometry.Pose2d;
import edu.wpi.first.math.geometry.Rotation2d;
import edu.wpi.first.wpilibj2.command.SubsystemBase;
import edu.wpi.first.wpilibj.smartdashboard.SmartDashboard;
import frc.robot.vision.Limelight;

public class LocalizationSubsystem extends SubsystemBase {
    private Pose2d currentPose;
    private final Limelight limelight;

    public LocalizationSubsystem() {
        this.currentPose = new Pose2d(0, 0, new Rotation2d());
        this.limelight = new Limelight(); // Assuming a Limelight class exists for vision processing
    }

    public void updateOdometry() {
        // Implement odometry update logic here based on encoders, gyros, etc.
        // Update currentPose based on the new calculations.
    }

    public void updateVision() {
        // Get data from Limelight and fuse it with currentPose
        if (limelight.hasTarget()) {
            Pose2d visionPose = limelight.getPose(); // Assuming getPose returns Pose2d
            // Fuse visionPose with currentPose, possibly using a Kalman filter or other algorithm
            this.currentPose = fusePoses(currentPose, visionPose);
        }
    }

    private Pose2d fusePoses(Pose2d odometryPose, Pose2d visionPose) {
        return odometryPose; // Placeholder, return the fused pose
    }

    public Pose2d getCurrentPose() {
        return currentPose;
    }

    @Override
    public void periodic() {
        updateOdometry();
        updateVision();
        SmartDashboard.putString("Robot Pose", currentPose.toString()); // Visualizing the robot's pose on the dashboard
    }
}
